{
  "name": "dark-pebble",
  "version": "0.0.0",
  "description": "Dark Pebble",
  "author": "Guillaume Tamboise",
  "license": "GPL-3.0-or-later",
  "scripts": {
    "deploy:qa": "serverless deploy -s qa",
    "deploy:prod": "serverless deploy -s prod",
    "deploy:info": "serverless info -v",
    "logs-tail-qa": "serverless logs -t -f myLambda -s qa",
    "logs-tail-prod": "serverless logs -t -f myLambda -s prod"
  },
  "dependencies": {
    "ask-sdk": "^2.3.0",
    "serverless": "^1.37.1"
  },
  "devDependencies": {},
  "repository": {
    "type": "git",
    "url": "git@github.com:djiti/skill-factory.git"
  }
}
